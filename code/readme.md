<!-- Create a new project folder.
Create a new GitHub repository (ensure it is private).
Add your Technical Mentor (TM) as a contributor to the project. (This is only for grading purposes. We promise we won't steal your code).
Regularly commit to the repository. -->
<!-- View the first movie's details:

The page should load the first movie's details including its poster, title, runtime, showtime, and available tickets.
The number of available tickets should be calculated by subtracting the number of tickets_sold from the theater's capacity.
Fetch the data from the following endpoint -->
<!-- See a menu of all movies:

Display a list of all movies on the left side of the page in the ul#films element when the page loads.
Optionally, style each film in the list by adding the classes film item to each li element.
Remove the placeholder li in the ul#films element either by editing the HTML file directly or using JavaScript.
Fetch the data from the following endpoint: -->
<!-- Buy a ticket for a movie:

After clicking the "Buy Ticket" button, the number of available tickets should decrease on the frontend.
Ensure that tickets cannot be bought if the showing is sold out (if there are 0 tickets available).
No persistence is needed for this feature. -->
<!-- To contribute to this project:

Fork the repository.
Create a new branch (git checkout -b feature-branch).
Make your changes.
Commit your changes (git commit -m 'Add some feature').
Push to the branch (git push origin feature-branch).
Create a new Pull Request. -->